import math
def find_next_square(sq):
    # Return the next square if sq is a square, -1 otherwise
    return -1 if math.sqrt(sq) != int(math.sqrt(sq)) else (math.floor(math.sqrt(sq)) + 1) ** 2 #this is not supposed to be commented
