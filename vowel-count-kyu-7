def get_count(sentence):
    num = 0
    if len(sentence) > 0:
        for i in range(len(sentence)):
            if sentence[i] in "aoeiuAOEIU":
                num += 1
    elif sentence == "" or sentence == None:
        num = 0
    else:
        num = 0
    return num
