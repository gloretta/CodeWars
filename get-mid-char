import math

def get_middle(s):
    if len(s) % 2 == 1:
        ind = math.floor((len(s) - 1) / 2)
        return s[ind]
    elif len(s) % 2 == 0:
        ind = math.floor(len(s) / 2)
        return s[ind - 1] + s[ind]
    elif len(s) == 1:
        return s
